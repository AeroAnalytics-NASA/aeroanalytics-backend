{
  "name": "aeroanalytics-backend",
  "version": "1.0.0",
  "description": "Azure Functions aero analytics API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "postinstall": "prisma generate",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@prisma/client": "^6.16.3",
    "@sendgrid/mail": "^8.1.6",
    "prisma": "^6.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.7.0",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.0",
    "typescript": "^5.9.3"
  }
}
